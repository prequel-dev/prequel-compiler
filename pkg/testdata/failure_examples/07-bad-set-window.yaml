rules:
  - cre:
      id: bad-window
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      set:
        # missing window
        event:
          source: cre.log.kafka
        match:
          - foo
          - bar
