rules:
  - cre:
      id: bad-order
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      set:
        event:
          source: cre.log.kafka
        order: # Use match instead
          - bar
