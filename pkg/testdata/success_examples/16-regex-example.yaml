rules:
  - cre:
      id: regex-example-2
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      set:
        event:
          source: cre.log.kafka
        match:
          - regex: "\\[emerg] (.+) still could not bind()"
