rules:
  - cre:
      id: regex-example-1
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      set:
        event:
          source: cre.log.kafka
        match:
          - regex: "emerg(.+)still could not bind()"
