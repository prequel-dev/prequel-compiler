rules:
  - cre:
      id: bad-yaml-literal-block
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      set:
        event:
          source: cre.log.kafka
        match:
          - regex: |
              "\\[emerg] (.+) still could not bind()"
