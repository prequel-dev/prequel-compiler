rules:
  - cre:
      id: seq-example-1
    metadata:
      id: rule-id
      hash: rule-hash
    rule:
      sequence:
        event:
          source: cre.log.kafka
        window: 1s
        order:
          - regex: "foo(.+)bar"
          - value: "test"
          - regex: "b(.+)az"
